syntax = "proto3";

package account_service;

enum LoginStatus{
  LoggedIn = 0;
  LoggedOut = 1;
  LogginFailed = 2;
}

enum AccountStatus{
  // zh: 停用
  AccountStopped = 0;
  // zh: 启用
  AccountActived = 1;
  // zh: 等待激活
  WaitingActivation = 2;
}
